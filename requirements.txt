PySide6>=6.7,<7
PySide6-Fluent-Widgets>=1.11.3
chatterbox-tts
# IMPORTANT: PyTorch/Torchaudio are installed separately as CUDA wheels by build/setup scripts.
# Do not add plain 'torch' here, or pip may replace CUDA Torch with a CPU build.
